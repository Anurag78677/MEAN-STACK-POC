export class Todo {
    id: number;
    title: string;
    content: string;
    // timeOfTheEvent:Date

    constructor(id, title, content) {
        this.id = id;
        this.title = title;
        this.content = content;
        // this.timeOfTheEvent = timeOfTheEvent;
    }

    // ,timeOfTheEvent
}
